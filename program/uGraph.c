#include<stdio.h>
#include"node.h"
#include"uGraph.h"

void create(uGraph* graph);

int main(int argc, char const *argv[])
{
    // 无向图邻接表


    return 0;
}

void create(uGraph* graph){
    // 初始化顶点数组
    for (int i = 0; i < graph->verNum; i++)
    {
        scanf("%d", &graph->vArray);
    }
    


}